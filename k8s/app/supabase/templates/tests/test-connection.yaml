apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "supabase.fullname" . }}-test-connection"
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "supabase.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
