apiVersion: v1
kind: Service
metadata:
  name: {{ include "metan.http.fullname" . }}
  labels:
    {{- include "metan.http.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metan.http.selectorLabels" . | nindent 4 }}
