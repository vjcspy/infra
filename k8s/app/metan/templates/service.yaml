apiVersion: v1
kind: Service
metadata:
  name: {{ include "metan.fullname" . }}
  labels:
    {{- include "metan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metan.selectorLabels" . | nindent 4 }}
