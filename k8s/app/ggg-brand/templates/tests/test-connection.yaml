apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ggg-brand.fullname" . }}-test-connection"
  labels:
    {{- include "ggg-brand.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ggg-brand.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
